<h2 class="section-title">投稿一覧</h2>
<% if params[:q].present? %>
  <p class="search-meta">
    「<strong><%= h params[:q] %></strong>」の検索結果（<%= @posts.size %>件）
  </p>
<% end %>

<% if user_signed_in? %>
  <div class="toolbar">
    <%= link_to "新規投稿", new_post_path, class: "btn btn-primary" %>
  </div>
<% end %>

<div class="container">
  <style>
  :root {
    --bg:#fff;
    --fg:#0f1419;
    --muted:#536471;
    --border:#e6ecf0;
    --accent:#1d9bf0;
    --shadow:0 8px 20px rgba(15,20,25,.05);
  }
  @media (prefers-color-scheme: dark) {
    :root {
      --bg:#000;
      --fg:#e6e9ea;
      --muted:#8b98a5;
      --border:#2f3336;
      --accent:#1d9bf0;
      --shadow:0 8px 20px rgba(0,0,0,.35);
    }
  }
  
  .container {
    max-width:820px;
    margin:20px auto;
    padding:0 16px;
    color:var(--fg);
  }
  
  .section-title {
    margin:0 0 10px;
    font-size:1.4rem;
    font-weight:700;
    color:#000 !important;
  }
  .search-meta { color:#666; margin:6px 0 12px; }
  .toolbar { margin:12px 0 16px; }
  
  /* 共通ボタン */
  .btn {
    display:inline-flex;
    align-items:center;
    justify-content:center;
    border-radius:12px;
    padding:10px 16px;
    font-weight:700;
    text-decoration:none;
    border:1px solid transparent;
    cursor:pointer;
    transition:box-shadow .15s, transform .02s;
  }
  .btn:active { transform:translateY(1px); }
  
  /* 新規投稿ボタン */
  .btn-primary {
    background:#0d6efd;
    color:#fff !important;   /* 常に白文字 */
  }
  .btn-primary:hover,
  .btn-primary:focus-visible {
    background:#0b5ed7;
    color:#fff !important;   /* ホバー時も白文字 */
    box-shadow:0 0 0 3px rgba(13,110,253,.25);
    outline:none;
  }
  
  /* 投稿カード */
  .post-card {
    border:1px solid var(--border);
    border-radius:16px;
    background:var(--bg);
    box-shadow:var(--shadow);
    padding:18px 20px;
    margin:16px auto;
  }
  .post-card:hover {
    border-color:var(--accent);
    box-shadow:0 0 0 3px rgba(29,155,240,.15);
  }
  
  .post-head {
    display:flex;
    gap:12px;
    align-items:center;
    justify-content:space-between;
    flex-wrap:wrap;
  }
  .post-title {
    margin:0;
    font-size:1.1rem;
    font-weight:800;
  }
  .post-title a {
    text-decoration:none;
    color:var(--fg);
  }
  .post-title a:hover,
  .post-title a:focus-visible {
    box-shadow:0 0 0 3px rgba(29,155,240,.15);
    outline:none;
    border-radius:8px;
    padding:2px 4px;
  }
  
  .post-meta { color:var(--muted); font-size:.95rem; white-space:nowrap; }
  .post-body { margin:10px 0 0; color:var(--fg); }
  .post-thumb { margin-top:10px; text-align:center; }
  .post-thumb img { border-radius:12px; max-width:100%; height:auto; }
  
  /* フッター */
  .post-footer {
    display:flex;
    justify-content:flex-end;
    align-items:center;
    gap:14px;
    margin-top:12px;
    font-size:.9rem;
    color:var(--muted);
  }
  .post-stat { white-space:nowrap; }
  .post-actions { display:flex; gap:8px; }
  
  /* 通常リンク */
  .action-link {
    text-decoration:none;
    color:var(--fg);
    font-weight:600;
  }
  .action-link:hover,
  .action-link:focus-visible {
    box-shadow:0 0 0 2px rgba(29,155,240,.25);
    border-radius:6px;
    outline:none;
  }
  
  /* 編集/削除ボタン */
  .btn-edit,
  .btn-delete {
    box-sizing:border-box;
    display:inline-block;
    padding:6px 12px;
    line-height:1.2;
    font-size:.9rem;
    font-weight:700;
    border-radius:6px;
    border:none;
    color:#fff;
    text-decoration:none;
    cursor:pointer;
    transition:background .15s ease, box-shadow .15s ease, transform .02s ease;
  }
  .btn-edit:active,
  .btn-delete:active { transform:translateY(1px); }
  
  /* 編集=緑 */
  .btn-edit { background:#28a745; }
  .btn-edit:hover,
  .btn-edit:focus-visible {
    background:#218838;
    box-shadow:0 0 0 3px rgba(40,167,69,.25);
    outline:none;
  }
  
  /* 削除=赤 */
  .btn-delete { background:#dc3545; }
  .btn-delete:hover,
  .btn-delete:focus-visible {
    background:#bb2d3b;
    box-shadow:0 0 0 3px rgba(220,53,69,.28);
    outline:none;
  }
  
  /* 汎用リンクhoverがボタンに影響しないようにする */
  .post-actions a:hover,
  .post-actions a:focus-visible {
    padding:0 !important;
    box-shadow:none;
  }

  .btn-title {
    background: none;
    border: none;
    padding: 0;
    font: inherit;
    font-size: 1.6rem;      /* ← ここでサイズを大きく指定 */
    font-weight: 800;       /* 太字で目立たせる */
    color: var(--fg);
    cursor: pointer;
    text-align: left;
    line-height: 1.3;
  }

  .btn-title:hover,
  .btn-title:focus-visible {
    text-decoration: underline;
    outline: none;
  }

  
  </style>

  <% @posts.each do |post| %>
    <article class="post-card">
      <header class="post-head">
      <h2 class="post-title">
      <%= button_to post.title,
                    post_path(post),
                    method: :get,
                    class: "btn-title" %>
      </h2>
        <small class="post-meta">
          by <%= post.user.name.presence || post.user.email %> ・
          <%= l post.created_at.in_time_zone('Asia/Tokyo'), format: :short %>
        </small>
      </header>
  
      <% if post.image.attached? %>
        <div class="post-thumb">
          <%= link_to post do %>
            <%= image_tag post.image.variant(resize_to_limit: [300, 300]),
                          alt: post.title %>
          <% end %>
        </div>
      <% end %>
  
      <p class="post-body"><%= truncate(post.body, length: 120) %></p>
  
      <!-- 右下の横並びエリア -->
      <div class="post-footer">
        <span class="post-stat">👍 いいね: <strong id="like-count-<%= post.id %>"><%= post.likes.count %></strong></span>
        <span class="post-stat">💬 コメント: <%= post.comments.size %></span>
  
        <% if user_signed_in? && post.user == current_user %>
        <span class="post-actions">
        <%= button_to "編集", edit_post_path(post),
        method: :get,
        class: "btn-edit" %>
            <%= button_to "削除", post,
                          method: :delete,
                          form: { data: { turbo_confirm: "削除しますか？" } },
                          class: "btn-delete" %>
          </span>
        <% end %>

      </div>
  
      <%= render "posts/like_section", post: post %>
    </article>
  <% end %>
  
</div>

<!-- ↓ ページの“下部”に置く -->
<div class="pagination-wrapper">
  <%= paginate @posts %>
</div>